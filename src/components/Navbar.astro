<nav>
  <div class="stars-container"></div>
  <div class="container mx-auto px-6 flex items-center justify-between">
    <a href="/" class="logo">
      <img src="/images/starmesh-logo.png" alt="Starmesh" />
    </a>
    
    <div class="nav-links">
      <a href="/HowItWorks" class="nav-link">How It Works</a>
      <a href="/PainPoints" class="nav-link">Pain Points</a>
      <a href="/UseCases" class="nav-link">Use Cases</a>
      <a href="/Pricing" class="nav-link">Pricing</a>
    </div>

    <div class="nav-buttons">
      <a href="https://starmesh-app.web.app" class="nav-link">Log In</a>
      <a href="#" class="cta-button">Request Access</a>
    </div>
  </div>
</nav>

<style>
  nav {
    height: 44px;
    display: flex;
    align-items: center;
    position: fixed;
    top: 10px;
    left: 50%;
    transform: translateX(-50%);
    max-width: 1200px;
    width: calc(100% - 96px);
    background-color: rgba(43, 45, 62, 0.95);
    backdrop-filter: blur(8px);
    border-radius: 18px;
    box-shadow: 0 4px 24px 0 rgba(0,0,0,0.08), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    border: 1px solid rgba(255,255,255,0.10);
    z-index: 1000;
    padding: 0.25rem 2rem;
  }

  .container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .logo {
    position: relative;
    z-index: 1;
  }

  .logo img {
    height: 40px;
    width: auto;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    margin: 0 2rem;
  }

  .nav-link {
    color: #B3acf2;
    text-decoration: none;
    font-size: 1rem;
    font-weight: 500;
    transition: all 0.2s ease;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
  }

  .nav-link:hover {
    color: #E4D6F5;
    background: rgba(255, 255, 255, 0.05);
  }

  .nav-buttons {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .cta-button {
    background: #8B5CF6;
    color: white;
    padding: 0.5rem 1.25rem;
    border-radius: 0.5rem;
    font-weight: 500;
    transition: all 0.2s ease;
    text-decoration: none;
  }

  .cta-button:hover {
    background: #7C3AED;
    transform: translateY(-1px);
  }

  @media (max-width: 768px) {
    nav {
      height: auto;
      padding: 1rem;
    }

    .container {
      flex-direction: column;
      gap: 1rem;
    }

    .nav-links {
      flex-direction: column;
      align-items: center;
      margin: 1rem 0;
      gap: 1rem;
    }

    .nav-buttons {
      flex-direction: column;
      width: 100%;
    }

    .nav-buttons .nav-link,
    .nav-buttons .cta-button {
      width: 100%;
      text-align: center;
    }
  }

  body {
    padding-top: 60px;
  }
</style> 